{
	"routes": [
		{
			"route": "/{*all}",
			"rewrite": "/api/{all}"
		}
	],
	"navigationFallback": {
		"rewrite": "/index.html",
		"exclude": [
			"/api/*",
			"/assets/*",
			"/favicon.ico"
		]
	}
}
